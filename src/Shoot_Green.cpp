#include "Shoot_Green.h"
/*
Shoot_Green::Shoot_Green()
{
    init();
    initTexture();
    initAnimation();

}

Shoot_Green::~Shoot_Green()
{
    //dtor
}

void Shoot_Green::draw(sf::RenderTarget& target, sf::RenderStates states) const
{
    target.draw(_shoot_sprite,states);
}

sf::FloatRect Shoot_Green::getBounds() const
{
    return _shoot_sprite.getGlobalBounds();
}

void Shoot_Green::init()
{
    _width_texture=15;
    _height_texture=15;
    _first_frame_of_sheet=sf::IntRect(320,75,_width_texture,_height_texture);
    _end_of_frames_sheet=380;
    _damage=1;
    _speed={0.f,0.f};
    _shoot_sprite.setPosition(300,400);
    _frame=0.f;
}
*/
