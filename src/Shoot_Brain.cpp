#include "Shoot_Brain.h"
/*
Shoot_Brain::Shoot_Brain()
{
    init();
    initTexture();
    initAnimation();
}

Shoot_Brain::~Shoot_Brain()
{
    //dtor
}

void Shoot_Brain::draw(sf::RenderTarget& target, sf::RenderStates states) const
{
    target.draw(_shoot_sprite,states);
}

sf::FloatRect Shoot_Brain::getBounds() const
{
    return _shoot_sprite.getGlobalBounds();
}

void Shoot_Brain::init()
{
    _width_texture=30;
    _height_texture=23;
    _first_frame_of_sheet=sf::IntRect(357,224,_width_texture,_height_texture);
    _end_of_frames_sheet=477;
    _damage=1;
    _speed={0.f,0.f};
    _shoot_sprite.setPosition(600,200);
    _frame=0.f;
}
*/
